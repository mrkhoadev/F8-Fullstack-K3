var type = new Typed(".textLine", {
    strings: [
        "Website của tôi",
        "bài tập về nhà của tôi",
        "bài tập về nhà tại F8",
    ],
    typeSpeed: 100,
    backSpend: 100,
    backDelay: 1000,
    loop: true,
});

import { handleFileFunction } from "./modules/function.js";
handleFileFunction()